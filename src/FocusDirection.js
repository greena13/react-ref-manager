/**
 * @typedef {{LEFT: string, RIGHT: string, UP: string, DOWN: string}} FocusDirection Constants used to indicate directions that a
 * ref collection can be iterated over.
 */
const FocusDirection = {
  LEFT: 'LEFT',
  RIGHT: 'RIGHT',
  UP: 'UP',
  DOWN: 'DOWN',
};

export default FocusDirection;
